# ==========================================
# LaLiga Predictor - Environment Configuration
# Rename this file to .env before running Docker
# ==========================================

# --- Database Configuration ---
# Docker will create a fresh PostgreSQL container using these details.
# You can leave these defaults or change the password for security.
DB_NAME=predictor
DB_USERNAME=postgres
DB_PASSWORD=change_this_password

# --- Security Secrets ---
# Used to sign JWT tokens. You can type any random long string here.
JWT_SECRET=insert_a_random_long_secret_string_here

# --- External Services (Required) ---
# 1. Brevo (Email Service): Get key from https://brevo.com (SMTP & API)
BREVO_API_KEY=xkeysib-your_brevo_api_key_here

# 2. Google OAuth2: Get credentials from Google Cloud Console
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret